# ======================================================================
# NextRole — Dépendances Python pour les DAGs Airflow
# ======================================================================

# Accès S3 / MinIO
boto3==1.34.0
apache-airflow-providers-amazon==8.19.0

# MongoDB depuis les DAGs
pymongo==4.7.1

# Traitement d'images et OCR
Pillow==10.3.0
azure-ai-documentintelligence==1.0.0
azure-core==1.30.0

# Manipulation de données
pandas==2.2.2
numpy==1.26.4

# Requêtes HTTP (appels API backend depuis les DAGs)
requests==2.32.0